import React from 'react';

import * as css from './css';

export default _ => (
  <div className="EmailFormDescription" style={css.EmailFormDescription()}>
    <div className="EmailFormTitle" style={css.EmailFormTitle()}>
      log in
    </div>

    <div className="EmailFormSubtitle" style={css.EmailFormSubtitle()}>
      to Track My Jobs
    </div>
  </div>
);

import * as u from './utils';

const f = u.f;

export const EmailFormDescription = _ => ({});

export const EmailFormTitle = _ => ({
  fontSize: f.textSizes.md,
  margin: '0 0 10px 0',
});

export const EmailFormSubtitle = _ => ({
  fontSize: f.textSizes.sm,
  margin: '0 0 10px 0',
});

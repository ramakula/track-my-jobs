export const EmailForm = _ => ({});

export const form = _ => ({
  display: 'flex',
  flexDirection: 'column',
  margin: '0 auto',
});

export const BottomContainer = _ => ({
  alignItems: 'center',
  display: 'flex',
  flexDirection: 'row',
  margin: '10px 0',
});

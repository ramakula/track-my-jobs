import * as c from '../../css/meta/colors';

export const Login = _ => ({
  height: '100%',
  margin: '25px 0',
  textAlign: 'center',
});

export const FormContainer = _ => ({
  border: `1px solid ${c.font.translucent}`,
  margin: '0 auto',
  padding: '30px',
  width: '300px',
});

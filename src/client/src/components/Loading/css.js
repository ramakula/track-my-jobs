import * as c from '../../css/meta/colors';

export const Loading = _ => ({
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  height: '100%',
});

export const colors = _ => ({
  font: c.font.dark,
});

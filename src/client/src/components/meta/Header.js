import React from 'react';

const style = {
  fontSize: '1.75rem',
};

const Header = _ => {
  return (
    <div className="Header" style={style}>
      Track My Jobs
    </div>
  );
};

export default Header;
